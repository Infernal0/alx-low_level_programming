#include "main.h"

/**
 * _islower - checks for lowercase character
 * @c: the character to check
 * Return: 1 if c is a lower case, 0 if not
 */
int_islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
